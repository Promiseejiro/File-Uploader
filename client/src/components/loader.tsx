//import ClipLoader from "react-spinners/ClipLoader"
interface Loading {
  loading:boolean;
}
function Loader({loading}:Loading ) {
  return (
/*<ClipLoader
        color={"#fff"}
       loading={loading}
        size={15}
        aria-label="Loading Spinner"
        data-testid="loader"
    className="mr-1"  />*/
    <div></div>
  )
}
export default Loader;
